# Cyclistic Capstone — Python dependencies
# ติดตั้งด้วย: pip install -r requirements.txt

pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # อ่าน/เขียนไฟล์ Parquet
matplotlib>=3.7
seaborn>=0.12
holidays>=0.40         # วันหยุดของรัฐ Illinois (Chicago)
jupyter>=1.0
notebook>=7.0
